<section class="content-header">
  <h1>
    Jobs
  </h1>
</section>

<section class="content">
  <% @jobs.each do |job| %>
    <%= render layout: "jobs/job", locals: { job: job } do %>
      <%= button_tag "Subscribe", class: "btn btn-warning btn-flat" %>
    <% end %>
  <% end %>
</section>
